spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gestao_financeira_transacoes_api
    username: devuser
    password: dev1234
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true